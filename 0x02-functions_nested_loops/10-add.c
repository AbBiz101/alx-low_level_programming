#include "main.h"
/**
 * add - adds two numbers
 * @num1: takes in a number
 * @num2: takes in a number
 * Return: returns the sum
 */
int add(int num1, int num2)
{
int result = num1 + num2;
return (result);
}
